Hopefully a better way of approaching modeling and simulation.

Build the documentation with:

$cd utilities && sh ./build_documentation

The results should be in the documenation folder.

Install with:

$python setup.py install
